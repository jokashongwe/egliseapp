{% extends 'base.html.twig' %}

{% block title %}TypeCulte index{% endblock %}

{% block body %}
    <h1>TypeCulte index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_culte in type_cultes %}
            <tr>
                <td>{{ type_culte.id }}</td>
                <td>{{ type_culte.libelle }}</td>
                <td>
                    <a href="{{ path('app_type_culte_show', {'id': type_culte.id}) }}">show</a>
                    <a href="{{ path('app_type_culte_edit', {'id': type_culte.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_culte_new') }}">Create new</a>
{% endblock %}
