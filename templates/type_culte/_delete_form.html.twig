<form method="post" action="{{ path('app_type_culte_delete', {'id': type_culte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_culte.id) }}">
    <button class="btn">Delete</button>
</form>
